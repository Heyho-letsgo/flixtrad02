
<header id="content-header">
  <h1><%= pluralize(@users.size, 'User') %></h1>
</header>


<ul id="users">
  <% @users.each do |user| %>
      <li>
        <%=	 user.id %> |
        <%= link_to user.name, user %> |
        <%= user.created_at.strftime('Membre depuis le:  %m/%d/%Y à  %H:%M') %>
             </li>
  <% end %>
</ul>